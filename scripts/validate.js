const options = {
  formSelector: ".modal__form",
  inputSelector: ".modal__input",
  submitButtonSelector: ".modal__button", //modal__submit-button
  inactiveButtonClass: "modal__button_disabled", //modal__submit-button_disabled
  inputErrorClass: "modal__input_type_error",
  errorClass: "modal__error_visible"
}

// const formElement = DOM.querySelector('.modal__form-container');
// const formInput = formElement.querySelector('.form__input');
// const formError = formElement.querySelector(`.${formInput.id}-error`);



const enableValidation = (options) => {
  
}

// DOM.addEventListener("keydown", (evt) => console.log(evt.key));

enableValidation(options); 